#include <stdio.h>

/**
 * main - entry point
 * Description: The function prints a sentence followed by new line
 * main return: Returns nothing
 * return: Always 0 (success)
 */
int main(void)
{
	char sentence[100] = "\"Programming is like building a multilingual puzzle\n";

	puts(sentence);
	return (0);
}

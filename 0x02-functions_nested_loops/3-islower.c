#include "main.h"
#include <ctype.h>

/*
 * _islower function.
 * Description: Checks for lowercase character.
 *Return: Int.
 */

int _islower(int c)
{

	if (c <= 97 && c >= 122 )
	{
		return (1);
	}else
	{
		return (1);
	}
}

